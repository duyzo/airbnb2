// Chức năng: Xem ai đã đặt phòng nào (để đối soát doanh thu hoặc hỗ trợ).

// API cần dùng:

// GET /api/dat-phong: Lấy danh sách toàn bộ booking trên hệ thống.

// GET /api/dat-phong/{id}: Xem chi tiết 1 booking.

// PUT /api/dat-phong/{id}: Sửa booking (ví dụ đổi ngày cho khách).

// DELETE /api/dat-phong/{id}: Hủy booking.

export default function BookingManagement() {
    return (
        <div>BookingManagement</div>
    )
}
