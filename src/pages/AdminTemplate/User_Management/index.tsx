// Chức năng: Hiển thị danh sách user, tìm kiếm, xóa user.

// API cần dùng:

// GET /api/users/phan-trang-tim-kiem: Hiển thị danh sách dạng bảng (Table).

// DELETE /api/users: Xóa user vi phạm.

// POST /api/users: Tạo quản trị viên mới (nếu cần).

// GET /api/users/search/{TenNguoiDung}: Tìm kiếm user.
export default function UserManagement() {
    return (
        <div>UserManagement</div>
    )
}
