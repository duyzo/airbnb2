// Chức năng: Khi người dùng chọn một địa điểm (ví dụ: Đà Lạt), trang này hiện ra tất cả phòng ở đó.

// API cần dùng:

// GET /api/phong-thue/lay-phong-theo-vi-tri: Lấy danh sách phòng dựa trên ID vị trí đã chọn.

// GET /api/vi-tri/{id}: Lấy thông tin chi tiết của vị trí (tên, hình ảnh) để hiển thị tiêu đề.
export default function ListingPage() {
    return (
        <div>index</div>
    )
}
