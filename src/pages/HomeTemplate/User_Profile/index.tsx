// Chức năng: Xem và chỉnh sửa thông tin cá nhân, avatar.

// API cần dùng:

// GET /api/users/{id}: Lấy thông tin user hiện tại.

// PUT /api/users/{id}: Cập nhật thông tin.

// POST /api/users/upload-avatar: Đổi ảnh đại diện.
export default function UserProfile() {
    return (
        <div>UserProfile</div>
    )
}
