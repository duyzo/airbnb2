// Chức năng: Xem danh sách các phòng mình đã đặt (để biết ngày đi, ngày về, trạng thái).

// API cần dùng:

// GET /api/dat-phong/lay-theo-nguoi-dung/{MaNguoiDung}: Lấy danh sách vé đã đặt của user đang đăng nhập.
export default function MyBookings() {
    return (
        <div>MyBookings</div>
    )
}
