// Chức năng: Hiển thị ảnh phòng, mô tả, tiện nghi, giá tiền, form đặt phòng và đánh giá.

// API cần dùng:

// GET /api/phong-thue/{id}: Lấy toàn bộ thông tin chi tiết của phòng.

// GET /api/binh-luan (giả định từ nhóm API BinhLuan): Lấy danh sách comment của phòng đó.

// POST /api/dat-phong: Gọi khi người dùng nhấn nút "Đặt phòng".
export default function RoomDetailPage() {
    return (
        <div>RoomDetailPage</div>
    )
}